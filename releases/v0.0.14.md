# camt053-loader-mt940 v0.0.14

Suite-wide lockstep release aligned to the camt053 suite v0.0.14 line. Dependency refresh only (the existing `camt053 >= 0.0.6, < 1` constraint already admits `0.0.14`); no functional changes in this package.
